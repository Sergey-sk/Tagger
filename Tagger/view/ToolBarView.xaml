<UserControl x:Class="Tagger.view.ToolBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Tagger.view"
             xmlns:nmsp="clr-namespace:Tagger"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <nmsp:WidthConverter x:Key="WidthConverter"/>

        <ControlTemplate x:Key="pinButton" TargetType="Button">
            <Border Name="pinBorder" Padding="5" Margin="5,0" Background="Transparent" CornerRadius="4">

                <VisualStateManager.VisualStateGroups>
                    <VisualStateGroup x:Name="CommonStates">
                        <VisualState x:Name="Normal">
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetName="contentTranslate"
                                 Storyboard.TargetProperty="Y"
                                 To="1.0" Duration="0:0:0.15"/>

                                <DoubleAnimation Storyboard.TargetName="contentScale"
                                 Storyboard.TargetProperty="ScaleX"
                                 To="1.0" Duration="0:0:0.15"/>
                                <DoubleAnimation Storyboard.TargetName="contentScale"
                                 Storyboard.TargetProperty="ScaleY"
                                 To="1.0" Duration="0:0:0.15"/>
                            </Storyboard>
                        </VisualState>

                        <VisualState x:Name="MouseOver">
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetName="contentScale"
                                 Storyboard.TargetProperty="ScaleX"
                                 To="1.15" Duration="0:0:0.15"/>
                                <DoubleAnimation Storyboard.TargetName="contentScale"
                                 Storyboard.TargetProperty="ScaleY"
                                 To="1.15" Duration="0:0:0.15"/>
                            </Storyboard>
                        </VisualState>

                        <VisualState x:Name="Pressed">
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetName="contentTranslate"
                                 Storyboard.TargetProperty="Y"
                                 To="1.5" Duration="0:0:0.05"/>

                                <DoubleAnimation Storyboard.TargetName="contentScale"
                                 Storyboard.TargetProperty="ScaleX"
                                 To="0.95" Duration="0:0:0.05"/>
                                <DoubleAnimation Storyboard.TargetName="contentScale"
                                 Storyboard.TargetProperty="ScaleY"
                                 To="0.95" Duration="0:0:0.05"/>
                            </Storyboard>
                        </VisualState>

                        <VisualState x:Name="Disabled"/>
                    </VisualStateGroup>
                </VisualStateManager.VisualStateGroups>

                <ContentPresenter x:Name="content"
                          VerticalAlignment="Center"
                          HorizontalAlignment="Center"
                          TextBlock.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                          RenderTransformOrigin="0.5,0.5">
                    <ContentPresenter.RenderTransform>
                        <TransformGroup>
                            <ScaleTransform x:Name="contentScale" ScaleX="1.0" ScaleY="1.0"/>
                            <TranslateTransform x:Name="contentTranslate" X="0" Y="0"/>
                        </TransformGroup>
                    </ContentPresenter.RenderTransform>
                </ContentPresenter>
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter TargetName="content" Property="TextBlock.Foreground" Value="{DynamicResource AccentTextFillColorPrimaryBrush}"/>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0"
              HorizontalAlignment="Left"
              VerticalAlignment="Center"
              Margin="5,0,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0"
                       FontSize="14"
                       VerticalAlignment="Center"
                       TextTrimming="CharacterEllipsis">
                 <Run Text="&#xEC50;" FontFamily="{StaticResource SymbolThemeFontFamily}"/>
                 <Run Text="{Binding Workspace.CurrentPath, StringFormat='Папка: {0}'}"/>

                <TextBlock.ToolTip>
                    <ToolTip Content="{Binding Workspace.CurrentPath}" ContentStringFormat="Папка: {0}"/>
                </TextBlock.ToolTip>
            </TextBlock>

            <Button Grid.Column="1"
                    Content="Сканировать"
                    Margin="8,0"
                    Style="{DynamicResource AccentButtonStyle}"
                    Command="{Binding Scanner.StartScanCommand}"/>
        </Grid>

        <StackPanel Grid.Column="1" Margin="0,0,5,0" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
            <Grid Margin="5,0">
                <TextBox x:Name="SearhTextBox"
                         Text="{Binding FileViewer.CurrentSearchText, UpdateSourceTrigger=PropertyChanged}"
                         FontSize="14"
                         VerticalAlignment="Stretch"
                         VerticalContentAlignment="Center"
                         Width="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource WidthConverter}, ConverterParameter=0.39}"
                         Padding="30,0,0,0">
                    <TextBox.InputBindings>
                        <KeyBinding Key="Enter" Command="{Binding FileViewer.TriggerSearchWithDebounceCommand}"/>
                    </TextBox.InputBindings>
                </TextBox>

                <Button Content="&#xE735;" FontFamily="{StaticResource SymbolThemeFontFamily}"
                        Height="{Binding ActualHeight, ElementName=SearhTextBox}"
                        Background="Transparent"
                        BorderThickness="0"
                        Template="{StaticResource pinButton}"
                        Command="{Binding Workspace.SaveCurrentSearchCommand}"/>

                <TextBlock Text="Поиск по названию/тегу"
                           FontSize="14"
                           Padding="33,0,0,0"
                           Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                           VerticalAlignment="Center"
                           IsHitTestVisible="False">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>

                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Text, ElementName=SearhTextBox}" Value="">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>

            <Button Content="&#xE721;" FontFamily="{StaticResource SymbolThemeFontFamily}"
                    Height="{Binding ActualHeight, ElementName=SearhTextBox}"
                    Style="{DynamicResource AccentButtonStyle}"
                    Command="{Binding FileViewer.TriggerSearchWithDebounceCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
